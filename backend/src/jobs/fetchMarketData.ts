/**
 * Job fetch Binance / Polygon — dijadwalkan dengan node-cron di slice Redis.
 */
export async function fetchMarketDataJob(): Promise<void> {
  try {
    // noop — slice 3
  } catch {
    // logged in real implementation
  }
}
